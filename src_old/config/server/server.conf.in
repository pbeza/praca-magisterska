# Server's listening port number

PORT = 4440

# Password protecting server's private key

PRIVATE_KEY_PASSWORD = "minipw"

# Server's private key path

PRIVATE_KEY_PATH = "../config/server/security/certificate.key"

# Path to server's CA certificate in PEM format

CERT_PATH = "../config/server/security/certificate.pem"

# Path to directory with configuration sets for clients
# TODO Auto fill absolute path using CMake.

CONFIG_SETS_DIR = "${CMAKE_SOURCE_DIR}/config/server/clients-config-sets"

# Path to directory with clients' cached packages

#PKG_CACHE_DIR_PREFIX = "/var/cache/apt/archives"
PKG_CACHE_DIR_PREFIX = "/tmp/mgr/test"
PKG_CACHE_DIR_APT_SUBDIR = "apt"
#PKG_CACHE_DIR_PACMAN_SUBDIR = "pacman"
