###############################################################################
# mySCM-srv security README                                                   #
###############################################################################

This directory contains files related to the security of the mySCM system image
generated by the myscm-srv, specifically to the certificate that is used to
sign mySCM system images. All mySCM system images should be singed with SSL
(x509) certificate using OpenSSL.

Files in this directory:

  * request.cnf            - Certificate Signing Request (CSR) configuration,
  * request.csr            - Certificate Signing Request (CSR) in PEM format,
  * certificate.pem        - x509 certificate in PEM format,
  * certificate.key        - certificate's encrypted private key in PEM format,
  * certificate.key.pkcs12 - certificate's encrypted private key in PKCS format,
  * certificate.key.pub    - certificate's encrypted public key in PEM format.

See also:

  * <https://stackoverflow.com/questions/10782826/digital-signature-for-a-file-using-openssl>
  * <https://serverfault.com/questions/9708/what-is-a-pem-file-and-how-does-it-differ-from-other-openssl-generated-key-file>
